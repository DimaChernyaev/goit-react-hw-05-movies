import styled from '@emotion/styled';

export const Message = styled.p`
  display: inline-flex;
  padding: 8px;
  background: rgba(255, 255, 255, 0.7);
  border-radius: 8px;
  margin-top: 20px;
  color: black;
  font-size: 18px;
  margin-left: auto;
  margin-right: auto;
`;
